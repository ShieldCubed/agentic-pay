{
  "_comment": "Copy this file to config.json and edit. This is the spend-limit policy and API key registry — the most important file to get right before letting any agent send real funds.",

  "apiKeys": {
    "sk-agent-replace-me": "example-agent-1"
  },

  "defaultPolicy": {
    "perTxCapUsd": 50,
    "dailyCapUsd": 200,
    "confirmationFloorUsd": 25,
    "allowedAssets": ["BTC", "ETH", "USDT", "USDC", "XRP", "XMR", "ZEC"]
  },

  "agentPolicies": {
    "example-agent-1": {
      "perTxCapUsd": 100,
      "dailyCapUsd": 500,
      "confirmationFloorUsd": 50,
      "allowedAssets": ["USDC", "XRP"],
      "allowlistAddresses": null
    }
  },

  "_staticPricesUsd_comment": "Used only for the built-in USD-equivalent spend-limit check. These are NOT live prices — replace railManager.priceUsd() with a real price feed before production use, or update these numbers regularly.",
  "staticPricesUsd": {
    "BTC": 65000,
    "ETH": 3200,
    "USDT": 1,
    "USDC": 1,
    "XRP": 0.55,
    "XMR": 160,
    "ZEC": 30
  }
}
